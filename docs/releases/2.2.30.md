# Cloth NeXt 2.2.30

Release date: 2026-08-13

This release replaces solver-frame-stepped Auto-Framing with continuous,
time-based viewport motion and restores durable Recovery for evaluated scenes
that deliberately bypass the optional persistent export cache.

See the repository root `RELEASE_NOTES.md` for the artist-facing summary.
