# Cloth NeXt 2.2.31

Release date: 2026-08-14

This release makes periodic Lumen checkpoints visible and resumable during a
running Bake even when the solver's status response omits the legacy
`saved_states` collection.

See the repository root `RELEASE_NOTES.md` for the artist-facing summary.
