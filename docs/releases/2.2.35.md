# Cloth NeXt 2.2.35

Release date: 2026-08-15

This release hardens recovery-path ownership, preserves validation across
Blender file loads, keys cache reuse to the exact solver release, and keeps
Companion status polling non-blocking.

See the repository root `RELEASE_NOTES.md` for the artist-facing summary.
