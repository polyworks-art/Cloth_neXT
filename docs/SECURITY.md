# Security of public Dev snapshots

Dev builds are public and unsupported, but never exempt from security and
distribution rules. Every Dev package receives source/final secret scans,
companion identity/hash validation, Blender package validation, and the
forbidden-artifact scan. Dev never contains PPF material, caches, blend files,
PC2 files, logs, credentials, tokens, local paths, or environment dumps.
