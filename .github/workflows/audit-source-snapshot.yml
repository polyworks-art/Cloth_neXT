name: Audit source snapshot

on:
  push:
    branches:
      - agent/full-addon-simulation-pipeline-audit
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  snapshot:
    if: github.head_ref == 'agent/full-addon-simulation-pipeline-audit' || github.ref_name == 'agent/full-addon-simulation-pipeline-audit'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Remove Git metadata
        run: rm -rf .git
      - name: Upload source snapshot
        uses: actions/upload-artifact@v4
        with:
          name: cloth-next-audit-source
          path: .
          include-hidden-files: true
          retention-days: 1
